# 미소병원 챗봇 통합 수동 검증 시나리오 및 가이드

본 문서는 개발된 미소병원 챗봇 시스템이 의도한 대로 동작하는지, 특히 **PII(개인정보) 마스킹 미들웨어**와 **안전한 DB 로깅**이 잘 작동하는지 사용자가 직접 검증해 볼 수 있는 시나리오를 제공합니다.

---

## 1. 테스트 환경 준비 (서버 실행)

테스트를 진행하기 위해서는 백엔드(챗봇 API) 서버가 실행되어 있어야 합니다. 터미널을 열고 다음 명령어를 실행해주세요.

```bash
cd /Users/hyeongju/code-test/ch-3/0903/chatbot-api
python3 app.py
```
> 서버가 정상적으로 실행되면 `Uvicorn running on http://0.0.0.0:8000` 문구가 출력됩니다.

서버가 켜진 상태에서 기존 홈페이지 화면(`/Users/hyeongju/code-test/ch-3/0903/miso-hospital-3tier/frontend/index.html`)을 웹 브라우저로 엽니다. (VSCode의 Live Server를 사용하거나 파일을 브라우저에 직접 드래그앤드롭)

화면 우측 하단의 파란색 **챗봇 아이콘(💬)**을 클릭하여 채팅창을 띄우고 아래 시나리오들을 차례로 입력해 봅니다.

---

## 2. 챗봇 프론트엔드 검증 시나리오 (마스킹 우회 방어 테스트)

### 시나리오 1: 표준 형태의 개인정보 입력
* **목적**: 정상적인 형식의 개인정보가 잘 필터링되는지 확인합니다.
* **입력 내용**:
  > "안녕하세요. 제 주민등록번호는 900101-1234567 이고 전화번호는 010-1234-5678 입니다. 기침이 나는데 어디로 가야하나요?"
* **예상 결과**:
  * 챗봇이 입력값을 내부적으로 `900101-[MASKED]` 와 `010-****-5678` 로 변환하여 처리합니다.
  * 챗봇의 답변은 "기침 증상은 내과 또는 이비인후과 진료를 권장합니다..." 형태로 정상적으로 출력되어야 합니다.

### 시나리오 2: 하이픈(-) 생략 입력 (우회 시도)
* **목적**: 악의적(혹은 실수)으로 구분자 없이 연속된 숫자를 입력했을 때 이를 잡아내는지 확인합니다.
* **입력 내용**:
  > "제 번호는 01012345678 이에요. 예약 가능한가요?"
* **예상 결과**:
  * 챗봇이 하이픈 없는 번호도 인식하여 내부적으로 `010-****-5678`로 마스킹합니다.
  * 챗봇의 답변은 예약 방법에 대한 정상적인 응답이 출력되어야 합니다.

### 시나리오 3: 띄어쓰기 남발 (우회 시도)
* **목적**: 숫자 중간중간에 공백을 넣어 정규식 탐지를 피하려는 시도를 차단하는지 확인합니다.
* **입력 내용**:
  > "제 주민번호는 9 0 0 1 0 1 - 1 2 3 4 5 6 7 입니다. 배가 많이 아파요."
* **예상 결과**:
  * 공백을 무시하고 숫자의 조합을 파악하여 `9 0 0 1 0 1-[MASKED]` 형태로 마스킹합니다.
  * 챗봇의 답변은 소화기내과에 대한 정상적인 응답이 출력되어야 합니다.

### 시나리오 4: 이름 마스킹
* **목적**: 문맥을 통한 이름 탐지 정규식이 작동하는지 확인합니다.
* **입력 내용**:
  > "제 이름은 홍길동입니다. 병원 위치가 어디인가요?"
* **예상 결과**:
  * 내부적으로 `제 이름은 ***입니다` 로 처리됩니다.
  * 챗봇의 답변은 병원 위치에 대한 정보가 정상 출력되어야 합니다.

---

## 3. 백엔드(DB) 안전 로깅 검증 가이드

채팅창에서 질문을 여러 번 입력한 후, 실제로 데이터베이스에 **마스킹된 텍스트**와 **암호화된 원문**이 잘 저장되었는지 터미널에서 확인합니다.

1. 새로운 터미널을 열고 `sqlite3`로 DB에 접속합니다.
```bash
cd /Users/hyeongju/code-test/ch-3/0903/chatbot-api
sqlite3 chatbot_logs.db
```

2. 터미널 프롬프트가 `sqlite>` 로 바뀌면 다음 쿼리를 입력해 저장된 데이터를 조회합니다.
```sql
.mode column
.headers on
SELECT id, masked_text, original_encrypted FROM logs;
```

* **예상 결과 (검증 완료 기준)**:
  * `masked_text` 컬럼에는 `900101-[MASKED]` 처럼 마스킹 처리된 평문 텍스트가 잘 보여야 합니다.
  * `original_encrypted` 컬럼에는 원문이 아닌, 해독 불가능한 형태의 바이트(알 수 없는 문자열들, 예: `gAAAAABn...`) 데이터가 들어가 있어야 합니다. (이로써 DB가 털려도 PII 유출을 방지함을 확인)

3. sqlite 터미널을 종료합니다.
```sql
.quit
```
