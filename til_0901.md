# 배움일기 (Today I Learned) - 0901

## 1. 오늘의 핵심 키워드
- **데코레이터 패턴 (Decorator Pattern)**: 파이썬에서 `@` 기호를 사용하여 기존 함수(예: RAG의 메인 로직)의 겉면을 감싸는 기술. 원본 코드를 한 줄도 수정하지 않고도 실행 전후의 입력과 출력을 가로채어 추가적인 작업(보안 감사 등)을 할 수 있는 매우 강력한 아키텍처 패턴.
- **비동기 처리 (ThreadPoolExecutor)**: 메인 프로그램(챗봇 응답)이 다른 작업(파일 저장, 암호화)을 기다리지 않도록, 백그라운드의 별도 스레드로 작업을 넘기는 기술. 이를 통해 시스템의 Latency(지연 시간)를 0으로 만들 수 있음.
- **무결성 해시 체인 (Hash Chain)**: 블록체인의 원리를 응용하여, 이전 로그의 해시값과 현재 데이터를 결합해 새로운 해시를 만드는 기법. 중간에 누군가 로그를 변조하면 연결 고리가 끊어져 즉시 발각됨.

## 2. 어려웠던 점 & 극복 과정
- **문제 상황:** 감사엔진을 연동하면서 매번 임시로 암호화 키를 생성하게 되니, 서버가 재시작될 때마다 과거에 기록된 로그들을 복호화할 수 없게 되는 문제 발생. 또한 터미널에서 대화형 테스트(CLI) 시 무한 루프에서 EOF 에러로 프로그램이 강제 종료되는 현상 발생.
- **시도한 방법:** 
  1. 키 문제 해결을 위해 `.env` 파일에 32바이트의 `AUDIT_ENCRYPTION_KEY`를 고정 발급하고 KMS(Key Management System) 방식으로 불러오도록 구조를 변경.
  2. 대화형 스크립트(`interactive_chat.py`)에 `try-except EOFError` 및 `traceback` 로직을 추가하여 강제 종료를 방지.
- **해결 및 배운 점:** 암호화 시스템에서는 단순한 암복호화 알고리즘뿐만 아니라, **'키를 어떻게 안전하게 영구적으로 관리할 것인가(Key Management)'**가 실무에서 훨씬 더 중요하다는 점을 깨달음. 에러 핸들링 역시 꼼꼼하게 처리해야 예기치 않은 시스템 셧다운을 막을 수 있음을 배움.

## 3. 오늘의 인사이트
- **결합도(Coupling)를 낮추는 설계의 힘**: 기존 RAG 에이전트와 감사엔진이라는 거대한 두 시스템을 결합할 때, 코드를 물리적으로 섞지 않고 '미들웨어(데코레이터)' 형태로 연동한 것이 신의 한 수였다. 서로의 내부 로직을 몰라도 되며, 에러가 발생해도 상대방에게 영향을 주지 않는 **'느슨한 결합(Loose Coupling)'**이 엔터프라이즈 아키텍처에서 얼마나 중요한지 직접 체감했다.
- **보안은 사용자 경험(UX)을 해치지 않아야 한다**: 비동기 로깅을 통해 보안 파이프라인이 백그라운드에서 아무리 무겁게 돌아가도 사용자에게는 즉시 답변이 나가게 구현했다. 훌륭한 보안 시스템은 사용자가 불편함을 느끼지 않게 뒤에서 조용히 일해야 한다는 것을 배웠다.
