<<<<<<< HEAD
## 신규 프로젝트 목표 설정 
- 팀 주제 : 사내 챗봇 로그 개인정보 탐지 서비스 
- 제공 로그·데이터 활용 방식 : 챗봇 대화 로그에서 PII를 탐지·마스킹하는 데 활용
- 중점 보안 역량 : 탐지 규칙 설계, 마스킹 처리

## 기존 프로젝트 분석 
병원 홈페이지를 만들고 만들어진 홈페이지에 대한 취약점 분석까지 진행됐음.
위치: 0903/미소병원 취약점 분석 보고서.md , 0903/miso-hospital-3tier/  

## 이후 플랜 및 추가 목표 
 0901에서 만들어진 interactive_chat.py(수동형 대화 테스트용 파일)을 기존 프로젝트에 추가로 챗봇을 만드려고 하고 있음. 
 홈페이지 우측 하단에 챗봇 버튼 추가 및 해당 버튼 클릭시 챗봇 창이 띄워지고 챗봇이 시작됨.

 
## 챗봇 제작시 설계 원칙과 설계 명세서 및 추가 조건
### 챗봇 입력 시 항상 PII 마스킹

**설계 원칙**: "항상"이라는 요구를 지키려면 마스킹을 챗봇 기능과 분리된 필수 미들웨어로 만들어, 우회 경로(다른 입력 필드, 다른 엔드포인트) 없이 모든 입력 처리 경로에 강제로 걸리게 해야 한다.

확인 필요(진행 전 결정):
- 챗봇의 용도(예: 증상 상담, FAQ 응대)와 백엔드로 사용할 LLM/API
- 마스킹 대상 PII 범위 — 최소 주민등록번호(`patients.rrn`과 동일 포맷 `000000-0000000`), 전화번호, 이름 정도는 필요
- 마스킹된 원문을 서버 로그에도 남기지 않을지, 마스킹 전 원문을 DB에 저장할지

해야 할 일 (결정 이후):
1. PII 탐지 함수(정규식 기반: 주민번호, 전화번호, 이메일 등) 작성 — 별도 유틸 모듈로 분리해 챗봇 외 다른 기능(예: 게시판 문의)에도 재사용 가능하게
2. 챗봇 입력이 LLM 호출/로그로 나가기 **직전**에 마스킹 적용 (사용자가 보는 화면 표시용 원문과, 외부로 나가는 마스킹본을 분리)
3. 마스킹 우회 테스트 케이스 작성 (예: 주민번호 중간에 공백/특수문자 삽입 시에도 탐지되는지)

# 파일 생성시 규칙 
에이전트는 설명을 할때 한글로 요약해서 설명을 해야함. 토큰 낭비를 절감하기 위한 목적.
interactive_chat.py 사용을 할때 0901에 있는 파일을 복사해서 0903에 가져오고 폴더 내에서 사용해야함. 
추가 적으로 필요한 파일이 있을시 똑같이 상위 폴더 경로에서 가져오되 기존 파일을 복사 해서 

# 프로젝트 진행시 에이전트가 작업을 하기 전 규칙
사용자가 작업 흐름을 파악, 데이터 파이프 라인을 확인하기 위해 현재 진행할 작업의 워크플로우나 데이터 흐름을 설명하는 마크다운 파일을 작성해야한다. mermaid로 보여주면 가시적으로 보기 좋음. 
작업을 진행할때 흐름을 보여주고 사용자에게 승인을 받은 후 진행해야함.
=======
## 목표
.ch-1/d05/3-2-rag.py 시스템에 감사엔진을 ADD-ON 하는것이 목표

## 주의 사항
- 계획 승인 후 진행 하고 한국어로 진행 
- 데이터가 제거 되면 안되니까 카피 형식으로 진행 

## step-1 : 관련 모듈 가져오기, 단 별도로 (완료)
0901에 복사 완료:
- 0901/rag-agent : ch-1/d05의 rag이랑 agent 부분 복사 완료
- 0901/audit-agent : ch-3/0831 의 src/audit_engine 복사 완료
감사엔진 만든것을 rag과 agent의 연동한다가 최종 목적- 알고 만 있기.

## step-2 : 연동 방식 선택 및 구현 (완료)
- **선택된 방식:** 데코레이터/미들웨어 방식 (Option 2)
- **구현 내용:** 
  1. `audit_decorator.py`를 생성하여 핵심 로직(`run_agent_with_trace`)에 `@audit_log` 데코레이터 부착.
  2. 메인 시스템 보호를 위한 강력한 예외 처리(Try-except) 적용.
  3. `audit-logs/audit_log.jsonl` 파일에 마스킹, 암호화, 만료일 산출, 해시체인이 적용된 로그 누적 저장.
  4. `masking.py`에 정규표현식(Regex)을 도입하여 문장형 프롬프트 내의 PII(이메일, 전화번호, 주민번호) 마스킹 및 악성 인젝션 쿼리 탐지 추가.
  5. 4가지 시나리오 검증용 테스트 스크립트(`test_scenarios.py`) 작성 및 검증 통과.

## step-3 : 시스템 안정성 및 편의성 고도화 (완료)
- **추가 5대 기능 구현 내역:**
  1. **보안 키 관리 (.env 도입)**: 재부팅되어도 과거 데이터를 영구적으로 조회할 수 있도록 마스터 암호화 키 적용.
  2. **복호화 뷰어 개발**: 관리자가 암호화된 JSONL 파일을 언제든 터미널에서 텍스트로 볼 수 있는 `decryption_viewer.py` 스크립트 구축.
  3. **실시간 위협 알림**: 악성 프롬프트 인젝션 감지 시 즉시 콘솔에 붉은색 경고 출력.
  4. **비동기 로깅**: 메인 시스템(RAG) 응답에 전혀 지연이 발생하지 않도록 `ThreadPoolExecutor`를 통한 백그라운드 처리 구축.
  5. **로그 로테이션**: 디스크 용량 관리 및 최적화를 위해 매 자정 기준으로 로그 파일을 분할 저장.
  6. **수동 대화형 테스트 CLI**: 사용자가 직접 자유롭게 해킹(인젝션)이나 개인정보를 입력해볼 수 있는 무한루프 채팅 스크립트(`interactive_chat.py`) 구축.
>>>>>>> 2003c8a698307c5a9c6708cc1120e086804a1b94
