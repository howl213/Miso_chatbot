## 신규 프로젝트 목표 설정 
- 팀 주제 : 사내 챗봇 로그 개인정보 탐지 서비스 
- 제공 로그·데이터 활용 방식 : 챗봇 대화 로그에서 PII를 탐지·마스킹하는 데 활용
- 중점 보안 역량 : 탐지 규칙 설계, 마스킹 처리

## 기존 프로젝트 분석 
병원 홈페이지를 만들고 만들어진 홈페이지에 대한 취약점 분석까지 진행됐음.
위치: 0903/미소병원 취약점 분석 보고서.md , 0903/miso-hospital-3tier/  

## 이후 플랜 및 추가 목표 
 0901에서 만들어진 interactive_chat.py(수동형 대화 테스트용 파일)을 기존 프로젝트에 추가로 챗봇을 만드려고 하고 있음. 
 홈페이지 우측 하단에 챗봇 버튼 추가 및 해당 버튼 클릭시 챗봇 창이 띄워지고 챗봇이 시작됨.

 
## 챗봇 제작시 설계 원칙과 설계 명세서 및 추가 조건
### 챗봇 입력 시 항상 PII 마스킹

**설계 원칙**: "항상"이라는 요구를 지키려면 마스킹을 챗봇 기능과 분리된 필수 미들웨어로 만들어, 우회 경로(다른 입력 필드, 다른 엔드포인트) 없이 모든 입력 처리 경로에 강제로 걸리게 해야 한다.

확인 필요(진행 전 결정):
- 챗봇의 용도(예: 증상 상담, FAQ 응대)와 백엔드로 사용할 LLM/API
- 마스킹 대상 PII 범위 — 최소 주민등록번호(`patients.rrn`과 동일 포맷 `000000-0000000`), 전화번호, 이름 정도는 필요
- 마스킹된 원문을 서버 로그에도 남기지 않을지, 마스킹 전 원문을 DB에 저장할지

해야 할 일 (결정 이후):
1. PII 탐지 함수(정규식 기반: 주민번호, 전화번호, 이메일 등) 작성 — 별도 유틸 모듈로 분리해 챗봇 외 다른 기능(예: 게시판 문의)에도 재사용 가능하게
2. 챗봇 입력이 LLM 호출/로그로 나가기 **직전**에 마스킹 적용 (사용자가 보는 화면 표시용 원문과, 외부로 나가는 마스킹본을 분리)
3. 마스킹 우회 테스트 케이스 작성 (예: 주민번호 중간에 공백/특수문자 삽입 시에도 탐지되는지)

# 파일 생성시 규칙 
에이전트는 설명을 할때 한글로 요약해서 설명을 해야함. 토큰 낭비를 절감하기 위한 목적.
interactive_chat.py 사용을 할때 0901에 있는 파일을 복사해서 0903에 가져오고 폴더 내에서 사용해야함. 
추가 적으로 필요한 파일이 있을시 똑같이 상위 폴더 경로에서 가져오되 기존 파일을 복사 해서 

# 프로젝트 진행시 에이전트가 작업을 하기 전 규칙
사용자가 작업 흐름을 파악, 데이터 파이프 라인을 확인하기 위해 현재 진행할 작업의 워크플로우나 데이터 흐름을 설명하는 마크다운 파일을 작성해야한다. mermaid로 보여주면 가시적으로 보기 좋음. 
작업을 진행할때 흐름을 보여주고 사용자에게 승인을 받은 후 진행해야함.