fastapi
uvicorn
slowapi
cryptography
google-generativeai
pydantic
pymysql
python-dotenv
# spacy 자체는 pip 패키지지만, pii_masking.py가 쓰는 한국어 모델(ko_core_news_md)은 별도
# 패키지라 여기 목록만으로는 안 딸려온다 - `python -m spacy download ko_core_news_md`으로
# 따로 설치해야 함(start.sh가 최초 실행 시 자동으로 확인·설치함). 없어도 서비스는 뜨지만
# 이름 마스킹 2차 안전망(트리거 단어 없는 이름 탐지)이 조용히 비활성 상태가 된다.
# [2026-09-14] sm(small)은 "미소병원"/"김치찌개"/"bot" 같은 일반 단어를 이름으로 오탐하는
# 사례가 실측으로 확인돼 md(medium)로 교체 - 용량은 더 크지만(69MB) 속도 차이는 무시할 수준.
spacy
